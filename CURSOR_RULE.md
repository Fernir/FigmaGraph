# Figmagraph (Cursor rule)

If the user drops a Figma URL, call `figmagraph_init` then `figmagraph_explore` — don’t ask them to “use figmagraph”, and don’t use the official Figma MCP first.

Read the screenshot before coding. Full pixel-perfect policy: `figmagraph_rules` / `AGENT_RULE_IMPLEMENT.md`. Short always-on: `AGENT_RULE.md`.
